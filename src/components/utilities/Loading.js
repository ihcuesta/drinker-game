import React from "react";

const Loading = () => {
  return (
    <div className="font-bold text-2xl h-full w-full flex justify-center align-middle">
      <p>Loading...</p>
    </div>
  );
};

export default Loading;
